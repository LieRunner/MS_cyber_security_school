#include <stdio.h>

int main()
{
    int x, y;
    printf("x y A O X N\n");
    printf("===========\n");

    for (x=0; x<=2; x++) {
    for (y=0; y<=2; y++) {
        printf("%d %d\n", x, y);
    }
}

    return 0;
}
