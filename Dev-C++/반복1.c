#include<stdio.h>

int main()
{
	int sval, eval, rval;
	printf("시작값을 입력하세요:"); scanf("%d", &sval);
	printf("종료값을 입력하세요:"); scanf("%d", &eval);
	
	
	
	rval = 0;
	int index = sval
    while((index >= sval)&&(index <=eval))  //수행조건이 들어감. 
	{
		
	}	
	rval = rval +sval; sval++;
	rval = rval +sval; sval++;
	rval = rval +sval; sval++;
	rval = rval +sval; sval++;
}
